<h2>회원 정보 보기<br />
  <a href="/">홈으로 가기</a>

</h2><br />
<table class = "table">
  <thead>
    <tr>
      <th scope="col">회원 번호</th>
      <th scope="col">이메일</th>
      <th scope="col">비밀번호</th>
      <th scope="col">비밀번호 수정</th>
      <th scope="col">회원 삭제</th>
    </tr>
  </thead>
  <% @user.each do |user| %>
  <tbody>
    <tr>
      <td> <%= user.id%> </td>
      <td> <%= user.email%> </td>
      <td> <%= user.password%> </td>
      <td> <a href="/user/edit/<%= user.id %>">비밀번호 수정</a> </td>
      <td> <a data-confirm = "정말 삭제하시겠어요?" data-method="delete" href="/user/delete/<%= user.id %>">회원 삭제</a> </td>
    </tr>
  </tbody>
  <%end%>
</table>
